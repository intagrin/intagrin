{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "IntaGrin ai.yaml",
  "$defs": {
    "AgentConfig": {
      "properties": {
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Short human-readable summary of this agent's purpose — shown in the Monitor dashboard graph.",
          "title": "Description"
        },
        "model_override": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Use a different LiteLLM model for this agent instead of the app-level model.primary.",
          "title": "Model Override"
        },
        "system_prompt_file": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Path to a Jinja2 template file rendered as this agent's system prompt.",
          "title": "System Prompt File"
        },
        "system_prompt_langfuse": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Langfuse prompt name to fetch as this agent's system prompt instead of a local file.",
          "title": "System Prompt Langfuse"
        },
        "system_prompt_module": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Python module path exposing a function that returns this agent's system prompt dynamically.",
          "title": "System Prompt Module"
        },
        "prompt_key": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Key used to look up this agent's prompt when multiple agents share a prompt source.",
          "title": "Prompt Key"
        },
        "required_scopes": {
          "description": "Scopes/permissions a caller must have (checked against session state) before this agent can run.",
          "items": {
            "type": "string"
          },
          "title": "Required Scopes",
          "type": "array"
        },
        "response_schema": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Dotted path to a Pydantic model this agent's final response is validated against; a corrector model retries once on violation.",
          "title": "Response Schema"
        },
        "auto_route": {
          "default": false,
          "description": "Enable semantic swarm routing (LLM-Bypass Group Chat)",
          "title": "Auto Route",
          "type": "boolean"
        },
        "lazy_load_tools": {
          "default": false,
          "description": "Enable semantic tool retrieval to reduce context window bloat",
          "title": "Lazy Load Tools",
          "type": "boolean"
        },
        "tools": {
          "description": "Tools available to this agent — local Python functions, MCP servers, OpenAPI wrappers, or references to root-level tools.",
          "items": {
            "anyOf": [
              {
                "$ref": "#/$defs/LocalToolConfig"
              },
              {
                "$ref": "#/$defs/MCPToolConfig"
              },
              {
                "$ref": "#/$defs/OpenAPIToolConfig"
              },
              {
                "$ref": "#/$defs/SandboxToolConfig"
              },
              {
                "$ref": "#/$defs/ToolReferenceConfig"
              }
            ]
          },
          "title": "Tools",
          "type": "array"
        },
        "handoffs": {
          "description": "Agent names this agent may conversationally transfer control to (compiled to a transfer_agent tool).",
          "items": {
            "type": "string"
          },
          "title": "Handoffs",
          "type": "array"
        },
        "delegations": {
          "description": "Agent names this agent may delegate a sub-task to. Compiles two tools: `delegate_task` (one sub-task, one isolated child engine — the delegating agent's own turn is never interrupted) and `delegate_to_many` (fan out N concurrent instances of the same sub-agent, one per instruction, for an item count only known at runtime — capped by circuit_breakers.max_parallel_fan_out).",
          "items": {
            "type": "string"
          },
          "title": "Delegations",
          "type": "array"
        },
        "routers": {
          "description": "Deterministic Python conditions checked before the LLM runs, to bypass it entirely when they fire.",
          "items": {
            "$ref": "#/$defs/RouterConfig"
          },
          "title": "Routers",
          "type": "array"
        },
        "spawns": {
          "anyOf": [
            {
              "$ref": "#/$defs/AgentSpawningConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Enables dynamic runtime agent creation: this agent gets a spawn_agent tool that creates a narrowly-scoped sub-agent (new system prompt, a subset of spawns.tool_pool, an inherited model) mid-session, runs it to completion in an isolated child engine, and returns the result as an ordinary tool result — it does not transfer control, so the creator's own turn is never interrupted and concurrent spawn_agent calls in one turn have nothing shared to race on. None (default) means this agent cannot spawn anything — today's behavior."
        }
      },
      "title": "AgentConfig",
      "type": "object",
      "additionalProperties": false
    },
    "AgentSpawningConfig": {
      "properties": {
        "tool_pool": {
          "description": "Closed allow-list of already-declared tool names a dynamically-created sub-agent's tools are drawn from — never a new tool implementation supplied at runtime. Must be a subset of this agent's own `tools:` (enforced at parse time); an agent can only hand off capabilities it already has, never escalate through creation. The literal string `\"*\"` is shorthand for every tool this agent itself has — expanded to that concrete list at parse time (AgentConfig's own subset validator), so it still can never grant more than the agent already holds.",
          "items": {
            "type": "string"
          },
          "title": "Tool Pool",
          "type": "array"
        },
        "model_pool": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "LiteLLM model identifiers a spawned agent may be assigned. None (default) means every spawned agent inherits the spawning agent's own resolved model — the spawning LLM never picks a model tier itself unless this is explicitly set.",
          "title": "Model Pool"
        },
        "max_creations_per_session": {
          "default": 3,
          "description": "Session-wide cap on how many agents this agent may dynamically create.",
          "minimum": 1,
          "title": "Max Creations Per Session",
          "type": "integer"
        },
        "requires_approval_on_first_action": {
          "default": true,
          "description": "Gate a spawned agent's very first tool call behind human approval (reuses the existing requires_approval/multi-approver /resume mechanism), regardless of whether that specific tool is itself approval-gated. Safe-by-default: opt out, not opt in.",
          "title": "Requires Approval On First Action",
          "type": "boolean"
        },
        "allow_recursive_spawning": {
          "default": false,
          "description": "Whether an agent spawned by this factory may itself spawn further agents (same tool_pool — no privilege growth), up to max_spawn_depth. Off by default.",
          "title": "Allow Recursive Spawning",
          "type": "boolean"
        },
        "max_spawn_depth": {
          "default": 1,
          "description": "Max recursive spawn depth, only consulted when allow_recursive_spawning is true.",
          "minimum": 1,
          "title": "Max Spawn Depth",
          "type": "integer"
        },
        "result_schema": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Dotted path to a Pydantic model a spawned agent's return_to_creator call must conform to. When set, return_to_creator's tool schema is derived from this model instead of a generic free-text `summary` field — steering the model via constrained tool-call decoding rather than just asking nicely — and the arguments are re-validated server-side before being accepted, self-healed via the same corrector-model retry already used for malformed tool arguments elsewhere. None (default) keeps today's free-text `summary` behavior.",
          "title": "Result Schema"
        },
        "on_complete": {
          "description": "State writes applied automatically once a spawned agent genuinely completes — return_to_creator or a final text response, never on a pause awaiting approval, never on a forced max-turns abort. Runs through the exact same reducer/state_schema pipeline as write_state, so validation and merge strategies apply identically — this is not a second, less-validated write path. Lets a tools[].available_when gate (or any other state-driven condition) unlock declaratively, instead of requiring the spawned agent's own instruction text to call write_state on the framework's behalf.",
          "items": {
            "$ref": "#/$defs/StateWriteAction"
          },
          "title": "On Complete",
          "type": "array"
        }
      },
      "required": [
        "tool_pool"
      ],
      "title": "AgentSpawningConfig",
      "type": "object",
      "additionalProperties": false
    },
    "AuthConfig": {
      "properties": {
        "type": {
          "default": "none",
          "description": "Authentication mode for `inta serve`/`inta monitor`: 'none' (no auth), 'api_key' (a single shared secret — sent as an `Authorization: Bearer` token for `inta serve`'s API, or as the password in HTTP Basic auth for `inta monitor`'s dashboard; the Basic auth username is not checked and can be any value, e.g. 'admin'), or 'custom' (delegates to a project-supplied verify_token function).",
          "enum": [
            "api_key",
            "custom",
            "none"
          ],
          "title": "Type",
          "type": "string"
        },
        "env_var": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": "INTAGRIN_API_KEY",
          "description": "Environment variable holding the API key, when type is 'api_key'. The server reads this at each auth check — export it before starting the server.",
          "title": "Env Var"
        },
        "custom_module": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Python module path (e.g. 'auth.custom') exposing `verify_token(token: str) -> bool | str`, when type is 'custom'. Must return the tenant id as a string for per-tenant session isolation — any other truthy value is rejected (401), not silently treated as a single shared tenant.",
          "title": "Custom Module"
        },
        "approver_env_var": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Environment variable holding a separate secret required (via the X-Approver-Key header) to approve a requires_approval tool call through /resume. Without this, the same credential that triggered the gated call can immediately approve it — set this to require a distinct reviewer credential from the requester's own session auth. Acts as the single default approver (id 'default') when `approvers` isn't set.",
          "title": "Approver Env Var"
        },
        "approvers": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Named approvers for multi-approver chains: maps an approver id (e.g. 'finance', 'security') to the environment variable holding that approver's own X-Approver-Key secret. A tool's `required_approvers` list names which of these ids must each sign off via /resume before it executes. Independent of `approver_env_var` — set both to keep a single default approver alongside named ones.",
          "title": "Approvers"
        }
      },
      "title": "AuthConfig",
      "type": "object",
      "additionalProperties": false
    },
    "CircuitBreakersConfig": {
      "properties": {
        "max_handoffs_per_session": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 25,
          "description": "Max allowed handoffs per session to prevent infinite loops",
          "title": "Max Handoffs Per Session"
        },
        "max_tool_failures_in_a_row": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 3,
          "description": "Max sequential tool failures before halting",
          "title": "Max Tool Failures In A Row"
        },
        "max_usd_cost_per_session": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Max USD cost per session before halting",
          "title": "Max Usd Cost Per Session"
        },
        "max_delegation_depth": {
          "default": 3,
          "description": "Max nested sub-agent delegation depth before rejecting further delegation",
          "title": "Max Delegation Depth",
          "type": "integer"
        },
        "max_delegation_turns": {
          "default": 15,
          "description": "Max turns a delegated sub-agent may take before it is forcefully aborted",
          "title": "Max Delegation Turns",
          "type": "integer"
        },
        "max_parallel_fan_out": {
          "default": 10,
          "description": "Max instructions a single delegate_to_many call may fan out to concurrently — each one runs a full isolated sub-agent to completion, so an LLM-chosen item count with no ceiling could spawn an unbounded number of child engines/LLM calls at once. A call over this limit is rejected outright (asking the caller to split the work into smaller batches) rather than silently truncated.",
          "minimum": 1,
          "title": "Max Parallel Fan Out",
          "type": "integer"
        },
        "max_parallel_tool_calls_per_turn": {
          "default": 10,
          "description": "Max ordinary (non-transfer) tool calls a single LLM completion may request that actually get executed concurrently in one turn. A model can request an arbitrary number of tool calls in one completion with no natural ceiling; calls beyond this limit are not executed — each gets a synthetic tool-result telling the model to split the work across turns — rather than silently gathered anyway, mirroring how a duplicate control-transfer call in the same turn is rejected rather than run.",
          "minimum": 1,
          "title": "Max Parallel Tool Calls Per Turn",
          "type": "integer"
        },
        "max_corrector_tokens": {
          "default": 1000,
          "description": "max_tokens applied to every self-healing corrector-model call — both malformed tool-call argument repair and response_schema/result_schema repair. These calls previously had no max_tokens set at all, making their cost genuinely unbounded; capping the output side bounds each individual corrector call's worst-case cost.",
          "minimum": 1,
          "title": "Max Corrector Tokens",
          "type": "integer"
        },
        "max_compression_batch_messages": {
          "default": 50,
          "description": "Max messages folded into long_term_memory per memory-compression LLM call. When more messages than this are evicted from the context window at once, compression runs in successive batches of this size, each folded into the summary in turn, instead of dumping an unbounded amount of evicted conversation into a single corrector-model prompt (the summary's own output side was already capped at 500 tokens; this bounds the input side).",
          "minimum": 1,
          "title": "Max Compression Batch Messages",
          "type": "integer"
        }
      },
      "title": "CircuitBreakersConfig",
      "type": "object",
      "additionalProperties": false
    },
    "ConditionFunctionConfig": {
      "properties": {
        "name": {
          "description": "Name a routers[].condition or tools[].available_when expression can call, e.g. `is_eligible(customer_tier, order_total)`. Must match the Python function name.",
          "title": "Name",
          "type": "string"
        },
        "module": {
          "description": "Python module path (e.g. 'tools.condition_functions') containing a pure, side-effect-free function named `name` that takes plain values and returns bool. Called with already-evaluated bare state-key names/literals as positional arguments — the function itself is never parsed as part of the condition grammar, only invoked by name, so this is the one way to express branching logic that the restricted condition grammar's comparisons/and/or/not can't reach (e.g. a regex match or a multi-field business rule) without falling back to an LLM handoff.",
          "title": "Module",
          "type": "string"
        }
      },
      "required": [
        "name",
        "module"
      ],
      "title": "ConditionFunctionConfig",
      "type": "object",
      "additionalProperties": false
    },
    "EpisodicMemoryConfig": {
      "properties": {
        "embedding_model": {
          "default": "text-embedding-3-small",
          "description": "Embedding model used to vectorize episode content and recall_episodes queries for semantic search. Defaults to match rag.embedding_model's default so a project using both features doesn't need two different embedding providers configured.",
          "title": "Embedding Model",
          "type": "string"
        },
        "scope": {
          "default": "session",
          "description": "Visibility scope for recorded episodes, independent of memory.shared_scope (a project may want the long_term_memory summary private per-session while episodic events are shared globally, or vice versa). 'session': only this session_id's own episodes. 'tenant': every session under the same authenticated caller/tenant prefix (same convention as memory.shared_scope: tenant). 'global': every session in the project, any tenant.",
          "enum": [
            "session",
            "tenant",
            "global"
          ],
          "title": "Scope",
          "type": "string"
        },
        "default_limit": {
          "default": 5,
          "description": "Default number of episodes recall_episodes returns when the caller doesn't pass an explicit limit.",
          "exclusiveMinimum": 0,
          "title": "Default Limit",
          "type": "integer"
        }
      },
      "title": "EpisodicMemoryConfig",
      "type": "object",
      "additionalProperties": false
    },
    "GuardrailsConfig": {
      "properties": {
        "banned_words": {
          "description": "Words/phrases that, if present in a user message or model output, are blocked before reaching the LLM or the user.",
          "items": {
            "type": "string"
          },
          "title": "Banned Words",
          "type": "array"
        },
        "mask_pii": {
          "default": false,
          "description": "Redact common PII patterns (emails, SSNs, card numbers) from messages before they're sent to the LLM or logged.",
          "title": "Mask Pii",
          "type": "boolean"
        },
        "system_safeguards": {
          "default": false,
          "description": "Append a built-in safety instruction to the system prompt discouraging harmful/off-policy behavior.",
          "title": "System Safeguards",
          "type": "boolean"
        },
        "custom_module": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Python module path exposing a custom guardrail check function, run in addition to the built-in checks above.",
          "title": "Custom Module"
        }
      },
      "title": "GuardrailsConfig",
      "type": "object",
      "additionalProperties": false
    },
    "ImportConfig": {
      "properties": {
        "path": {
          "description": "Path to another ai.yaml-shaped file to merge in (agents, tools, workflows, reducers).",
          "title": "Path",
          "type": "string"
        },
        "namespace": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Prefix added to imported agent/workflow names, to avoid collisions with the importing project.",
          "title": "Namespace"
        }
      },
      "required": [
        "path"
      ],
      "title": "ImportConfig",
      "type": "object",
      "additionalProperties": false
    },
    "LocalToolConfig": {
      "properties": {
        "name": {
          "description": "Tool name exposed to the LLM for tool-calling — must match the Python function name.",
          "title": "Name",
          "type": "string"
        },
        "module": {
          "description": "Python module path (e.g. 'tools.custom_tools') containing the function named `name`.",
          "title": "Module",
          "type": "string"
        },
        "requires_approval": {
          "default": false,
          "description": "Require human-in-the-loop approval before this tool actually executes.",
          "title": "Requires Approval",
          "type": "boolean"
        },
        "required_approvals": {
          "default": 1,
          "description": "Number of distinct approvers (see server.auth.approvers) who must each approve via /resume before this tool executes — 1 (default) matches today's single-approval behavior. Ignored unless requires_approval is true.",
          "minimum": 1,
          "title": "Required Approvals",
          "type": "integer"
        },
        "required_approvers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Specific approver ids (keys of server.auth.approvers) that must each sign off, instead of any `required_approvals` approvers. Ignored unless requires_approval is true; when set, overrides required_approvals with len(required_approvers).",
          "title": "Required Approvers"
        },
        "available_when": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "State condition (same restricted grammar as routers[].condition — bare state-key names, comparisons, and/or/not; no method calls or attribute access) gating whether this tool is even offered to the agent this turn. Unlike a prompt instruction asking the model not to call a tool yet, the tool is structurally absent from its schema until the condition is true — re-checked server-side on every call regardless, the same defense-in-depth already applied to tool_pool and every other schema-driven gate in this codebase. None (default) means always available, today's behavior.",
          "title": "Available When"
        },
        "untrusted_output": {
          "default": false,
          "description": "Mark this tool's return value as untrusted (may contain LLM-directed instructions injected by a third party — the 'lethal trifecta' pattern: untrusted content + access to private data/state + a way to exfiltrate). False by default for local tools, since they're developer-authored Python — set true for one that fetches external content (e.g. a web scraper). The moment any tool call with untrusted_output=true succeeds, state['_untrusted_content_ingested'] is set true for the rest of the session; reference it from another tool's available_when (e.g. 'not _untrusted_content_ingested') to withhold a sensitive tool once this session has seen untrusted content, or from a router condition to force a review handoff.",
          "title": "Untrusted Output",
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "module"
      ],
      "title": "LocalToolConfig",
      "type": "object",
      "additionalProperties": false
    },
    "MCPToolConfig": {
      "properties": {
        "name": {
          "description": "Tool name exposed to the LLM for tool-calling.",
          "title": "Name",
          "type": "string"
        },
        "type": {
          "const": "mcp",
          "description": "Discriminator — must be the literal string 'mcp'.",
          "title": "Type",
          "type": "string"
        },
        "command": {
          "description": "Executable used to launch the MCP server subprocess (e.g. 'npx').",
          "title": "Command",
          "type": "string"
        },
        "args": {
          "description": "Arguments passed to `command` when launching the MCP server.",
          "items": {
            "type": "string"
          },
          "title": "Args",
          "type": "array"
        },
        "requires_approval": {
          "default": false,
          "description": "Require human-in-the-loop approval before this tool actually executes.",
          "title": "Requires Approval",
          "type": "boolean"
        },
        "required_approvals": {
          "default": 1,
          "description": "Number of distinct approvers (see server.auth.approvers) who must each approve via /resume before this tool executes — 1 (default) matches today's single-approval behavior. Ignored unless requires_approval is true.",
          "minimum": 1,
          "title": "Required Approvals",
          "type": "integer"
        },
        "required_approvers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Specific approver ids (keys of server.auth.approvers) that must each sign off, instead of any `required_approvals` approvers. Ignored unless requires_approval is true; when set, overrides required_approvals with len(required_approvers).",
          "title": "Required Approvers"
        },
        "available_when": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "State condition (same restricted grammar as routers[].condition) gating whether this tool is even offered to the agent this turn — see LocalToolConfig.available_when for the full explanation. None (default) means always available, today's behavior.",
          "title": "Available When"
        },
        "untrusted_output": {
          "default": true,
          "description": "Mark this tool's return value as untrusted — see LocalToolConfig.untrusted_output for the full explanation. True by default for MCP tools, since they reach an external server outside the project's own trust boundary; set false only for an MCP server you fully control and trust.",
          "title": "Untrusted Output",
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "type",
        "command",
        "args"
      ],
      "title": "MCPToolConfig",
      "type": "object",
      "additionalProperties": false
    },
    "MemoryConfig": {
      "properties": {
        "type": {
          "default": "sliding_window",
          "description": "Conversation history backend: 'sliding_window'/'buffer' (in-process, lost on restart), 'sqlite' (local file), 'postgres', 'redis', or 'custom'.",
          "enum": [
            "sliding_window",
            "buffer",
            "sqlite",
            "postgres",
            "redis",
            "custom"
          ],
          "title": "Type",
          "type": "string"
        },
        "max_messages": {
          "default": 20,
          "description": "Number of most-recent messages kept in context for sliding_window/buffer memory.",
          "exclusiveMinimum": 0,
          "title": "Max Messages",
          "type": "integer"
        },
        "db_path": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": ".ai/memory.db",
          "description": "SQLite database file path, relative to the project root, when type is 'sqlite'.",
          "title": "Db Path"
        },
        "connection_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Direct connection URL for postgres/redis, e.g. 'postgresql://...' or 'redis://...'. Takes precedence over env_var.",
          "title": "Connection Url"
        },
        "env_var": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Environment variable holding the connection URL for postgres/redis (e.g. 'DATABASE_URL', 'REDIS_URL'), used if connection_url isn't set directly.",
          "title": "Env Var"
        },
        "custom_module": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Python module path exposing a CustomCheckpointer class, when type is 'custom'.",
          "title": "Custom Module"
        },
        "shared_scope": {
          "default": "session",
          "description": "Scope for the long_term_memory summary _compress_memory produces: 'session' (default — today's behavior, private to one session_id), 'tenant' (shared across every session under the same authenticated caller/tenant), or 'global' (shared across every session in the project, any tenant). Persisted to a new shared_memory table (sqlite/postgres only, same scope as run_logs) and merged into a session's long_term_memory on initialize(). Last-write-wins across concurrent sessions writing the same scope — no merge/versioning.",
          "enum": [
            "session",
            "tenant",
            "global"
          ],
          "title": "Shared Scope",
          "type": "string"
        }
      },
      "title": "MemoryConfig",
      "type": "object",
      "additionalProperties": false
    },
    "ModelConfig": {
      "properties": {
        "primary": {
          "description": "LiteLLM model identifier used for this agent/app (e.g. 'openai/gpt-4o-mini', 'anthropic/claude-3-5-sonnet'). Required.",
          "title": "Primary",
          "type": "string"
        },
        "fallback": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Model to retry with if the primary model call fails (rate limit, outage, etc.).",
          "title": "Fallback"
        },
        "variants": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/ModelVariantConfig"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "A/B or canary model routing: split traffic across weighted model variants instead of always using `primary`. Assignment is deterministic per session_id (sticky for the whole conversation, never flips mid-session) via a weighted hash. None (default) means every session uses `primary`, exactly today's behavior.",
          "title": "Variants"
        },
        "temperature": {
          "default": 0.2,
          "description": "Sampling temperature passed to the LLM, 0.0 (deterministic) to 2.0 (most random).",
          "maximum": 2.0,
          "minimum": 0.0,
          "title": "Temperature",
          "type": "number"
        },
        "max_tokens": {
          "default": 1500,
          "description": "Maximum tokens the LLM may generate in a single completion.",
          "exclusiveMinimum": 0,
          "title": "Max Tokens",
          "type": "integer"
        },
        "use_cache": {
          "default": false,
          "description": "Enable semantic caching to save API costs",
          "title": "Use Cache",
          "type": "boolean"
        },
        "guardrails": {
          "$ref": "#/$defs/GuardrailsConfig",
          "description": "Content-safety checks applied to this model's inputs/outputs."
        }
      },
      "required": [
        "primary"
      ],
      "title": "ModelConfig",
      "type": "object",
      "additionalProperties": false
    },
    "ModelVariantConfig": {
      "properties": {
        "model": {
          "description": "LiteLLM model identifier for this variant.",
          "title": "Model",
          "type": "string"
        },
        "weight": {
          "description": "Relative weight for traffic splitting — weights don't need to sum to 1.",
          "exclusiveMinimum": 0.0,
          "title": "Weight",
          "type": "number"
        }
      },
      "required": [
        "model",
        "weight"
      ],
      "title": "ModelVariantConfig",
      "type": "object",
      "additionalProperties": false
    },
    "OpenAPIToolConfig": {
      "properties": {
        "name": {
          "description": "Tool name exposed to the LLM for tool-calling.",
          "title": "Name",
          "type": "string"
        },
        "type": {
          "const": "openapi",
          "description": "Discriminator — must be the literal string 'openapi'.",
          "title": "Type",
          "type": "string"
        },
        "url": {
          "description": "URL of the OpenAPI/Swagger spec IntaGrin generates a tool wrapper from.",
          "title": "Url",
          "type": "string"
        },
        "auth_env": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Environment variable holding a bearer token/API key sent with requests to this API.",
          "title": "Auth Env"
        },
        "requires_approval": {
          "default": false,
          "description": "Require human-in-the-loop approval before this tool actually executes.",
          "title": "Requires Approval",
          "type": "boolean"
        },
        "required_approvals": {
          "default": 1,
          "description": "Number of distinct approvers (see server.auth.approvers) who must each approve via /resume before this tool executes — 1 (default) matches today's single-approval behavior. Ignored unless requires_approval is true.",
          "minimum": 1,
          "title": "Required Approvals",
          "type": "integer"
        },
        "required_approvers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Specific approver ids (keys of server.auth.approvers) that must each sign off, instead of any `required_approvals` approvers. Ignored unless requires_approval is true; when set, overrides required_approvals with len(required_approvers).",
          "title": "Required Approvers"
        },
        "available_when": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "State condition (same restricted grammar as routers[].condition) gating whether this tool is even offered to the agent this turn — see LocalToolConfig.available_when for the full explanation. None (default) means always available, today's behavior.",
          "title": "Available When"
        },
        "untrusted_output": {
          "default": true,
          "description": "Mark this tool's return value as untrusted — see LocalToolConfig.untrusted_output for the full explanation. True by default for OpenAPI-derived tools, since they call an external API outside the project's own trust boundary; set false only for an API you fully control and trust.",
          "title": "Untrusted Output",
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "type",
        "url"
      ],
      "title": "OpenAPIToolConfig",
      "type": "object",
      "additionalProperties": false
    },
    "RAGConfig": {
      "properties": {
        "docs_dir": {
          "default": "docs",
          "description": "Directory of documents to index for retrieval, relative to the project root.",
          "title": "Docs Dir",
          "type": "string"
        },
        "embedding_model": {
          "default": "text-embedding-3-small",
          "description": "Embedding model used to vectorize documents and queries.",
          "title": "Embedding Model",
          "type": "string"
        },
        "top_k": {
          "default": 4,
          "description": "Number of chunks retrieved per query.",
          "title": "Top K",
          "type": "integer"
        },
        "chunk_size": {
          "default": 500,
          "description": "Maximum characters per indexed chunk.",
          "title": "Chunk Size",
          "type": "integer"
        },
        "chunk_overlap": {
          "default": 50,
          "description": "Characters of overlap between consecutive chunks.",
          "title": "Chunk Overlap",
          "type": "integer"
        },
        "hyde": {
          "default": false,
          "description": "Enable Hypothetical Document Embeddings for advanced retrieval",
          "title": "Hyde",
          "type": "boolean"
        }
      },
      "title": "RAGConfig",
      "type": "object",
      "additionalProperties": false
    },
    "RateLimitConfig": {
      "properties": {
        "max_requests_per_window": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Max /chat, /chat/stream, /resume, or /stream requests one authenticated caller (tenant) may make within `window_seconds`. None (default) means unlimited. Enforced by counting that caller's rows in the run_logs audit table, so it only applies when memory.type is 'sqlite' or 'postgres' — the same scope run_logs itself has.",
          "title": "Max Requests Per Window"
        },
        "window_seconds": {
          "default": 60,
          "description": "Rolling window, in seconds, that max_requests_per_window is measured over.",
          "exclusiveMinimum": 0,
          "title": "Window Seconds",
          "type": "integer"
        },
        "max_cost_per_caller_per_day": {
          "anyOf": [
            {
              "type": "number"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Max total USD cost one authenticated caller may accrue in a rolling 24h window, summed from run_logs.cost_delta. None (default) means unlimited.",
          "title": "Max Cost Per Caller Per Day"
        },
        "max_tokens_per_caller_per_day": {
          "anyOf": [
            {
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Max total tokens one authenticated caller may consume in a rolling 24h window, summed from run_logs.tokens_delta. None (default) means unlimited.",
          "title": "Max Tokens Per Caller Per Day"
        }
      },
      "title": "RateLimitConfig",
      "type": "object",
      "additionalProperties": false
    },
    "RootRouterConfig": {
      "properties": {
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "What this router decides",
          "title": "Description"
        },
        "module": {
          "description": "Python module containing a route(state) function",
          "title": "Module",
          "type": "string"
        },
        "possible_targets": {
          "description": "Strict list of allowed target agents to maintain determinism and UI visualization",
          "items": {
            "type": "string"
          },
          "title": "Possible Targets",
          "type": "array"
        }
      },
      "required": [
        "module"
      ],
      "title": "RootRouterConfig",
      "type": "object",
      "additionalProperties": false
    },
    "RouterConfig": {
      "properties": {
        "condition": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Python evaluation string against state (e.g., \"balance < 0\")",
          "title": "Condition"
        },
        "custom_module": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Module to execute deterministic routing logic",
          "title": "Custom Module"
        },
        "target": {
          "description": "Agent to route to when this router fires.",
          "title": "Target",
          "type": "string"
        }
      },
      "required": [
        "target"
      ],
      "title": "RouterConfig",
      "type": "object",
      "additionalProperties": false
    },
    "SandboxToolConfig": {
      "description": "Runs agent-generated code in an isolated subprocess (see runtime/sandbox.py for exactly\nwhat is and isn't isolated — process/resource/environment isolation, NOT a filesystem or\nnetwork security boundary). The missing piece between the coding-agent template's coder/\nverifier loop (which writes and reviews code) and actually running code an LLM produced.",
      "properties": {
        "name": {
          "description": "Tool name exposed to the LLM for tool-calling.",
          "title": "Name",
          "type": "string"
        },
        "type": {
          "const": "sandbox",
          "description": "Discriminator — must be the literal string 'sandbox'.",
          "title": "Type",
          "type": "string"
        },
        "language": {
          "default": "python",
          "description": "Interpreter used to run the submitted code.",
          "enum": [
            "python",
            "bash"
          ],
          "title": "Language",
          "type": "string"
        },
        "timeout_seconds": {
          "default": 10,
          "description": "Wall-clock limit before the sandboxed process is killed. Also used as the POSIX CPU-time rlimit (see runtime/sandbox.py) — a no-op on platforms without the `resource` module (Windows).",
          "maximum": 300,
          "minimum": 1,
          "title": "Timeout Seconds",
          "type": "integer"
        },
        "max_memory_mb": {
          "anyOf": [
            {
              "minimum": 1,
              "type": "integer"
            },
            {
              "type": "null"
            }
          ],
          "default": 256,
          "description": "POSIX address-space rlimit (RLIMIT_AS) applied to the sandboxed process, in megabytes. None disables the memory limit entirely. A no-op on platforms without the `resource` module (Windows).",
          "title": "Max Memory Mb"
        },
        "requires_approval": {
          "default": false,
          "description": "Require human-in-the-loop approval before this tool actually executes.",
          "title": "Requires Approval",
          "type": "boolean"
        },
        "required_approvals": {
          "default": 1,
          "description": "Number of distinct approvers (see server.auth.approvers) who must each approve via /resume before this tool executes — 1 (default) matches today's single-approval behavior. Ignored unless requires_approval is true.",
          "minimum": 1,
          "title": "Required Approvals",
          "type": "integer"
        },
        "required_approvers": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Specific approver ids (keys of server.auth.approvers) that must each sign off, instead of any `required_approvals` approvers. Ignored unless requires_approval is true; when set, overrides required_approvals with len(required_approvers).",
          "title": "Required Approvers"
        },
        "available_when": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "State condition (same restricted grammar as routers[].condition) gating whether this tool is even offered to the agent this turn — see LocalToolConfig.available_when for the full explanation. None (default) means always available, today's behavior.",
          "title": "Available When"
        },
        "untrusted_output": {
          "default": true,
          "description": "Mark this tool's return value as untrusted — see LocalToolConfig.untrusted_output for the full explanation. True by default: sandboxed code's stdout/stderr can be influenced by whatever the executed code does (including content it read from elsewhere), so it's treated the same as an external tool result by default.",
          "title": "Untrusted Output",
          "type": "boolean"
        }
      },
      "required": [
        "name",
        "type"
      ],
      "title": "SandboxToolConfig",
      "type": "object",
      "additionalProperties": false
    },
    "ServerConfig": {
      "properties": {
        "auth": {
          "$ref": "#/$defs/AuthConfig",
          "description": "Authentication configuration for `inta serve`/`inta monitor`. Defaults to no authentication — set this before exposing either beyond localhost."
        },
        "webhook_url": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Webhook URL for async HITL notifications",
          "title": "Webhook Url"
        },
        "webhook_secret_env_var": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Env var containing secret to authenticate webhooks",
          "title": "Webhook Secret Env Var"
        },
        "rate_limit": {
          "$ref": "#/$defs/RateLimitConfig",
          "description": "Per-caller rate limiting/usage quotas for the API server. All thresholds default to unlimited — opt in per project."
        }
      },
      "title": "ServerConfig",
      "type": "object",
      "additionalProperties": false
    },
    "StateReducerConfig": {
      "properties": {
        "key": {
          "description": "Shared state key this reducer applies to.",
          "title": "Key",
          "type": "string"
        },
        "strategy": {
          "default": "overwrite",
          "description": "How concurrent/repeated writes to `key` are combined: 'overwrite' (last write wins), 'append' (to a list), or 'deep_merge' (for dicts).",
          "enum": [
            "overwrite",
            "append",
            "deep_merge"
          ],
          "title": "Strategy",
          "type": "string"
        }
      },
      "required": [
        "key"
      ],
      "title": "StateReducerConfig",
      "type": "object",
      "additionalProperties": false
    },
    "StateWriteAction": {
      "properties": {
        "key": {
          "description": "State key to write.",
          "title": "Key",
          "type": "string"
        },
        "value": {
          "description": "Value to write — the same JSON-ish value write_state accepts.",
          "title": "Value"
        }
      },
      "required": [
        "key",
        "value"
      ],
      "title": "StateWriteAction",
      "type": "object",
      "additionalProperties": false
    },
    "ToolReferenceConfig": {
      "description": "Reference a tool or MCP/OpenAPI provider declared at the root level.",
      "properties": {
        "name": {
          "description": "Name of a tool or MCP/OpenAPI provider declared at the ai.yaml root.",
          "title": "Name",
          "type": "string"
        },
        "available_when": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "State condition (same restricted grammar as routers[].condition) gating whether this tool is even offered to this agent this turn — see LocalToolConfig.available_when for the full explanation. This is the field that actually matters in practice: a per-agent `tools:` entry is usually a name-reference to a root-level tool, and availability is inherently agent-specific (the same globally-declared tool might be gated for one agent and unrestricted for another). None (default) means always available, today's behavior.",
          "title": "Available When"
        }
      },
      "required": [
        "name"
      ],
      "title": "ToolReferenceConfig",
      "type": "object",
      "additionalProperties": false
    },
    "VoteConfig": {
      "properties": {
        "strategy": {
          "default": "majority",
          "description": "How a 'vote' workflow task's branch answers become one result: 'majority' compares branch outputs directly with no extra LLM call; 'llm_judge' asks the model to pick or synthesize the best answer from all branch outputs.",
          "enum": [
            "majority",
            "llm_judge"
          ],
          "title": "Strategy",
          "type": "string"
        },
        "min_agreement": {
          "default": 0.5,
          "description": "Minimum fraction (0.0-1.0) of branches that must agree for 'majority' to declare a winner. Below this, no answer is guessed — the task reports 'no consensus' plus all branch outputs instead.",
          "title": "Min Agreement",
          "type": "number"
        }
      },
      "title": "VoteConfig",
      "type": "object",
      "additionalProperties": false
    },
    "WorkflowTask": {
      "properties": {
        "name": {
          "description": "Task name, used for logging/tracing this step of the workflow.",
          "title": "Name",
          "type": "string"
        },
        "type": {
          "default": "sequential",
          "description": "'sequential' runs sub-tasks/agent one after another; 'parallel' runs them concurrently and appends every branch's result; 'vote' runs them concurrently like 'parallel' but aggregates branch answers into one consensus result (see `vote`).",
          "enum": [
            "sequential",
            "parallel",
            "vote"
          ],
          "title": "Type",
          "type": "string"
        },
        "agent": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Agent that executes this task.",
          "title": "Agent"
        },
        "instruction": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Instruction given to `agent` for this task.",
          "title": "Instruction"
        },
        "tasks": {
          "anyOf": [
            {
              "items": {
                "$ref": "#/$defs/WorkflowTask"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Nested sub-tasks, when this task is a grouping node rather than a leaf agent call.",
          "title": "Tasks"
        },
        "vote": {
          "anyOf": [
            {
              "$ref": "#/$defs/VoteConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Aggregation settings for a 'vote' task (ignored otherwise); defaults to majority voting with 50% minimum agreement when omitted."
        }
      },
      "required": [
        "name"
      ],
      "title": "WorkflowTask",
      "type": "object",
      "additionalProperties": false
    }
  },
  "properties": {
    "version": {
      "description": "ai.yaml schema/format version.",
      "title": "Version",
      "type": "string"
    },
    "name": {
      "description": "Project name.",
      "title": "Name",
      "type": "string"
    },
    "description": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Short human-readable summary of what this app does.",
      "title": "Description"
    },
    "state_schema": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Global JSON schema module path for Typed Shared State",
      "title": "State Schema"
    },
    "max_session_budget_usd": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Global hard cost ceiling per session",
      "title": "Max Session Budget Usd"
    },
    "imports": {
      "description": "Other ai.yaml-shaped files to merge into this one.",
      "items": {
        "$ref": "#/$defs/ImportConfig"
      },
      "title": "Imports",
      "type": "array"
    },
    "circuit_breakers": {
      "$ref": "#/$defs/CircuitBreakersConfig",
      "description": "Thresholds that halt a runaway session — handoff loops, tool failure streaks, USD cost, delegation depth."
    },
    "model": {
      "$ref": "#/$defs/ModelConfig",
      "description": "Default LLM configuration, used by any agent that doesn't set model_override."
    },
    "memory": {
      "$ref": "#/$defs/MemoryConfig",
      "description": "Conversation history backend used by all agents in this app."
    },
    "rag": {
      "anyOf": [
        {
          "$ref": "#/$defs/RAGConfig"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Vector retrieval configuration — when set, auto-registers a search_knowledge_base tool."
    },
    "episodic_memory": {
      "anyOf": [
        {
          "$ref": "#/$defs/EpisodicMemoryConfig"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Discrete, structured, individually queryable event records (e.g. \"user prefers window seats\", \"booking BK-4471 failed: card declined\") — distinct from the single blended long_term_memory prose summary and from rag's document/knowledge-base retrieval. When set, auto-registers remember_episode/recall_episodes tools. Stored in a sqlite/postgres episodes table (memory.type must be 'sqlite' or 'postgres' — same scope as shared_memory/run_logs); a no-op elsewhere."
    },
    "tools": {
      "description": "Tools declared at the root level, available to be referenced by name from any agent.",
      "items": {
        "anyOf": [
          {
            "$ref": "#/$defs/LocalToolConfig"
          },
          {
            "$ref": "#/$defs/MCPToolConfig"
          },
          {
            "$ref": "#/$defs/OpenAPIToolConfig"
          },
          {
            "$ref": "#/$defs/SandboxToolConfig"
          }
        ]
      },
      "title": "Tools",
      "type": "array"
    },
    "agents": {
      "additionalProperties": {
        "$ref": "#/$defs/AgentConfig"
      },
      "description": "The agents that make up this app, keyed by name.",
      "title": "Agents",
      "type": "object"
    },
    "routers": {
      "additionalProperties": {
        "$ref": "#/$defs/RootRouterConfig"
      },
      "description": "Root-level deterministic routers, keyed by name — evaluated before any agent runs.",
      "title": "Routers",
      "type": "object"
    },
    "workflows": {
      "additionalProperties": {
        "items": {
          "$ref": "#/$defs/WorkflowTask"
        },
        "type": "array"
      },
      "description": "Named multi-step task sequences, run via `inta run <workflow>` rather than conversationally.",
      "title": "Workflows",
      "type": "object"
    },
    "reducers": {
      "description": "How concurrent writes to shared state keys are combined.",
      "items": {
        "$ref": "#/$defs/StateReducerConfig"
      },
      "title": "Reducers",
      "type": "array"
    },
    "condition_functions": {
      "description": "Named, pure Python predicate functions that routers[].condition and tools[].available_when expressions may call by name (e.g. `is_eligible(customer_tier)`), for branching logic the restricted condition grammar's bare comparisons/and/or/not can't express on their own — without forcing an LLM handoff just to make a deterministic decision.",
      "items": {
        "$ref": "#/$defs/ConditionFunctionConfig"
      },
      "title": "Condition Functions",
      "type": "array"
    },
    "telemetry": {
      "description": "Tracing backends to export spans to.",
      "items": {
        "enum": [
          "langfuse",
          "otel"
        ],
        "type": "string"
      },
      "title": "Telemetry",
      "type": "array"
    },
    "server": {
      "$ref": "#/$defs/ServerConfig",
      "description": "`inta serve`/`inta monitor` configuration — auth, webhooks."
    },
    "default_agent": {
      "description": "Agent that receives the first message in a new session.",
      "title": "Default Agent",
      "type": "string"
    }
  },
  "required": [
    "version",
    "name",
    "model",
    "memory",
    "default_agent"
  ],
  "type": "object",
  "additionalProperties": false
}
